<Window x:Class="CheckupAddIn.Views.PresetPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Dlg_PresetPicker_Title}"
        Width="320" Height="260"
        MinWidth="280" MinHeight="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        SizeChanged="Window_SizeChanged"
        Background="{DynamicResource CheckupWindowBackground}"
        Foreground="{DynamicResource CheckupPrimaryText}"
        FontFamily="Segoe UI" FontSize="12">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CheckupAddIn2024;component/Resources/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Button">
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment"   Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"
                                                  Focusable="False"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="{DynamicResource Dlg_PresetPicker_Label}"
                   Margin="0,0,0,8"/>
        <ListBox Grid.Row="1"
                 x:Name="PickerList"
                 Margin="0,0,0,12"
                 DisplayMemberPath="Name"
                 ScrollViewer.CanContentScroll="False"
                 SelectionChanged="PickerList_SelectionChanged"
                 MouseDoubleClick="PickerList_DoubleClick"
                 Background="{DynamicResource CheckupComboItemBackground}"
                 Foreground="{DynamicResource CheckupPrimaryText}"
                 BorderBrush="{DynamicResource CheckupButtonBorder}"
                 BorderThickness="1"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Content="{DynamicResource Btn_Apply}"
                    Width="70" Height="28" Margin="0,0,8,0"
                    IsDefault="True" IsEnabled="False"
                    Click="OK_Click"
                    FontSize="11"
                    Background="{DynamicResource CheckupButtonBackground}"
                    Foreground="{DynamicResource CheckupButtonForeground}"
                    BorderBrush="{DynamicResource CheckupButtonBorder}"/>
            <Button Content="{DynamicResource Btn_Cancel}"
                    Width="70" Height="28"
                    IsCancel="True"
                    Click="Cancel_Click"
                    FontSize="11"
                    Background="{DynamicResource CheckupButtonBackground}"
                    Foreground="{DynamicResource CheckupButtonForeground}"
                    BorderBrush="{DynamicResource CheckupButtonBorder}"/>
        </StackPanel>
    </Grid>
</Window>
