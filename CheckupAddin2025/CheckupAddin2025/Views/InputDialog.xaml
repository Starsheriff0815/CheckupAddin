<Window x:Class="CheckupAddIn.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Dlg_SavePreset_Title}"
        Width="320"
        SizeToContent="Height"
        MinHeight="120"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource CheckupWindowBackground}"
        Foreground="{DynamicResource CheckupPrimaryText}"
        FontFamily="Segoe UI" FontSize="12">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment"   Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"
                                              Focusable="False"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="16,16,16,16">
        <TextBlock Text="{DynamicResource Dlg_SavePreset_Label}" Margin="0,0,0,8"/>
        <TextBox x:Name="NameBox"
                 Height="28" Margin="0,0,0,12"
                 KeyDown="NameBox_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Btn_Apply}" Width="70" Height="28" Margin="0,0,8,0"
                    IsDefault="True" Click="OK_Click"
                    FontSize="11"
                    Background="{DynamicResource CheckupButtonBackground}"
                    Foreground="{DynamicResource CheckupButtonForeground}"
                    BorderBrush="{DynamicResource CheckupButtonBorder}"/>
            <Button Content="{DynamicResource Btn_Cancel}" Width="70" Height="28"
                    IsCancel="True" Click="Cancel_Click"
                    FontSize="11"
                    Background="{DynamicResource CheckupButtonBackground}"
                    Foreground="{DynamicResource CheckupButtonForeground}"
                    BorderBrush="{DynamicResource CheckupButtonBorder}"/>
        </StackPanel>
    </StackPanel>
</Window>
